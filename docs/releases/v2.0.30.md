# LyricsX Next 2.0.30

新增「歌词 → 字体与颜色 → 逐字独立配色」。开启后分别设置已唱到、未唱到部分的颜色；只影响带逐字时间的当前歌词，同时应用于主窗口与悬浮窗。翻译和下一句继续使用辅助文字颜色。

未唱到的自定义颜色不会再乘以原有的调暗透明度。关闭独立配色恢复原来的主歌词颜色与明暗进度，保留已经选择的两种颜色。支持即时保存、恢复默认，以及只在可见时运行的动态预览。

验证：214 项测试通过，包括逐字开始、中间和结束时的实际 RGB 像素检查及配置持久化。
