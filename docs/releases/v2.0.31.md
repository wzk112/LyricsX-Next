# LyricsX Next 2.0.31

- 封面短暂缺失时保留上一帧，再以模糊淡入淡出切换；背景等待异步模糊准备完成后交替，避免先闪成纯色。
- 主界面的歌名、歌手与封面分开过渡，取消整个播放区域一起淡入的行为。歌词进入过渡从第一帧开始，保持切歌时的滚动位置重置。
- 补充窗口首次显示后的 EDR 能力刷新，覆盖视图先挂载、屏幕信息后到达的情况。
- 验证逐字独立配色仍输出扩展亮度：青色、3.5 倍设置在离屏浮点渲染中蓝色峰值约 3.0；实际屏幕亮度由系统余量决定。

- 自定义字体增加有界字面比例校正，行高测量包含系统备用字形；主歌词、翻译和下一句采用一致的行框对齐，固定尺寸模式同步预留空间。

验证：217 项完整测试通过，包含自定义字体主歌词与翻译的实际像素居中、分隔、边界，以及换字体时取消旧几何动效的检查。排版测试覆盖系统字体、Georgia、Menlo 和 Helvetica Neue 的中英混排及不同宽度。
