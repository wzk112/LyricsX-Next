# LyricsX Next 2.0.28

补充修复悬浮窗歌词已经换行、窗口仍停留在旧高度而截断翻译的问题。2.0.27 的材质遮罩同步没有覆盖歌词视图与窗口控制器独立更新的路径。

歌词视图现在会在实际排版高度变化时直接通知窗口，覆盖等待点、单双行歌词及翻译切换，不依赖鼠标隐藏再显示。保留控制器的提前测量；两条路径共用同一尺寸应用函数。

窗口缩放增加单次、可取消的结束校验，窗口被遮挡或快速切换时仍能收敛到最新高度，不引入持续轮询。保留固定宽度、顶部锚点和现有非线性动效。

新增长外语句加翻译、等待与歌词反复切换的视图高度上报测试；完整测试验证后发布。
