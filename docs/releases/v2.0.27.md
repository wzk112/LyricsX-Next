# LyricsX Next 2.0.27

修复悬浮窗在等待歌词、双行歌词和翻译之间切换时，玻璃背景及遮罩可能滞留在旧高度的问题。现在窗口缩放时同步更新材质、渐变与遮罩，不再依赖鼠标经过或后续布局才能恢复。

窗口动画结束时校验实际目标尺寸，避免请求尺寸缓存阻止后续修正。保留顶部位置、固定宽度、非线性缩放和现有歌词动效。

验证包含后台通知安全、歌词替换、连续打断缩放及逐步检查玻璃遮罩边界。移除同步修复的对照测试能够检出旧尺寸残留。

下载并解压安装包，退出旧版后将 LyricsX Next.app 替换到应用程序文件夹；已有设置与歌词缓存保留。
